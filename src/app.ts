import { createApp } from 'vue';
import '@nutui/nutui-taro/dist/style.css';
import '@/assets/icon/iconfont.css';
import './app.scss';

const App = createApp({
  // 可以使用所有的 Vue 生命周期方法
  // 对应 onLaunch
  onLaunch(options) {
  },
});

export default App;
